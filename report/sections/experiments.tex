\section{Results}
\label{sec:experiments}

This section compares task formulations and training sources. Recall the dataset names: \textbf{LightwheelAI Baseline} (60 public full-task demonstrations), \textbf{Teleop} (manually collected subtasks), \textbf{Auto} (balanced auto-generated successful subtasks), and \textbf{Teleop+Auto} (their merge).

Every completed run uses the same 100-scene reference seed set (\texttt{pick\_orange\_reference\_100\_v1}) in the same order; the randomised quantities are the top-camera pose and the orange positions. Differences between models are therefore attributable to policy and formulation rather than to easier or harder sampled scenes.

\subsection{Full-Task Outcomes Across Formulations}

\Cref{fig:orange_outcome_distribution} reports final plate occupancy for every model on the shared 100-seed set under a 5000-step budget, via two metrics: the \emph{full-task success rate} (fraction of episodes ending with all three oranges placed --- whether the task is solved) and the \emph{mean final occupancy} (average oranges placed out of three --- crediting partial progress, separating headway from early stalls). Bars are grouped by training source and, within the two custom sources, by formulation (\emph{monotask} end-to-end vs.\ orchestrated \emph{subtask}).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{orange_outcome_distribution.pdf}
    \caption{Final plate occupancy per model on the shared 100 seeded episodes: the share ending with 0--3 oranges placed, with the mean above each bar.}
    \label{fig:orange_outcome_distribution}
\end{figure}

\paragraph{Policy backbone.} \textbf{[Placeholder: interpretation of the ACT vs.\ SmolVLA baseline comparison to be finalised.]}

\paragraph{Decomposition helps partial progress, not full-task completion.} On the same trajectories, the subtask formulation's benefit is in partial progress, not finishing all three. On Teleop it raises mean occupancy sharply (1.11 to 1.77) while leaving full-task success flat (18\% to 20\%): checkpoints convert empty and one-orange episodes into two-orange ones, rarely into three. On Teleop+Auto the monotask model is in fact stronger on both metrics (mean 1.50 vs.\ 1.42, full-task 26\% vs.\ 13\%). Decomposition thus secures more oranges on average on Teleop but does not raise full-task completion.

\paragraph{Subtasking rarely fails completely.} The clearest advantage is on the weakest outcome --- an episode with no orange placed. The subtask models almost always place at least one (97\% of Teleop episodes, 84\% on Teleop+Auto), whereas the monotask models leave many empty (only 57\% and 73\% reach one). Target redirection is the likely reason: a monotask policy can fixate on one hard-to-grasp orange and burn the episode, while the controller detects the repeated GRASP timeout and redirects to another. The decomposed formulation thus rarely fails totally, even when it does not complete the task.

\paragraph{Aggregating autonomous data did not help.} Adding the Auto episodes to Teleop regressed the subtask model on both metrics (full-task 20\% to 13\%, mean 1.77 to 1.42). In-distribution successful subtasks were thus not sufficient to improve full-task behaviour; the subtask-level analysis below isolates what degraded.

\subsection{The Grasp Bottleneck}
\label{sec:failure_modes}

\textbf{The wall is the grasp.} For the orchestrated (subtask) models --- the only ones whose grasp attempts are real, budgeted events rather than a heuristic reconstruction --- only about \textbf{37\%} of grasp attempts secure an orange (\Cref{fig:failure_modes}); the rest time out without acquiring one. Everything \emph{downstream} of a secured grasp is reliable: lift and place each succeed 80--92\% of the time, so once an orange is held it reaches the plate \textbf{87\%} of the time on Teleop (\Cref{tab:grasp_chain}). Local retry --- re-grasping the \emph{same} orange after a slip, the orchestrator's \textbf{Level-1} recovery --- is rarely the deciding factor: only 12 of the 186 placed-and-secured oranges needed a second grasp. The cap on full-task success is therefore upstream, at acquiring the orange at all: explicit monitoring cannot place an orange the policy never grasps.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{failure_modes.pdf}
    \caption{Per-subtask outcome composition for the two orchestrated models (share of genuine attempts). A grasp secures an orange only about 37\% of the time; once it does, lift and place each succeed 80--92\%. Monotask models are excluded --- their grasp attempts are reconstructed heuristically.}
    \label{fig:failure_modes}
\end{figure}

\begin{table}[H]
    \centering
    \small
    \caption{The grasp$\rightarrow$place chain, from privileged physics. \emph{Grasp success} (secured / attempts) is subtask-only: the monotask trace reconstructs grasps heuristically, so it has no trustworthy grasp rate. \emph{Secured} means a force-confirmed grasp for the subtask models and a lift for the monotask ones (which do not force-gate before lifting); \emph{place once secured} is the fraction of those oranges that reach the plate.}
    \label{tab:grasp_chain}
    \begin{tabular}{llccc}
        \toprule
        Training set & Formulation & Grasp success & Secured oranges/ep & Place once secured \\
        \midrule
        Teleop      & Monotask & --- & 1.4 & 77\% \\
        Teleop      & Subtask  & 38\% & 2.1 & 87\% \\
        \midrule
        Teleop+Auto & Monotask & --- & 1.7 & 87\% \\
        Teleop+Auto & Subtask  & 37\% & 1.9 & 73\% \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{What Redirection Buys}
\label{sec:formulation}

The orchestrator's distinctive ability is to \textbf{give up on a hard orange and try a different one}. The subtask models attempt about \textbf{2.7} distinct oranges per episode (\Cref{tab:redirection}) --- more than the $\sim$2 they ever secure --- and after a GRASP times out, a \emph{different} orange is subsequently placed \textbf{31\%} of the time on Teleop (20\% on Teleop+Auto): the orchestrator's \textbf{Level-2} recovery. The payoff shows at the weakest outcome: the Teleop subtask model ends an episode with \emph{no} orange placed only \textbf{3\%} of the time, against \textbf{43\%} for its monotask twin. Unable to switch targets, the end-to-end policy burns episodes on oranges it cannot grasp.

\begin{table}[H]
    \centering
    \small
    \caption{Redirection (Level-2) and its outcome. \emph{Oranges tried} is the distinct grasp targets the orchestrator requested per episode; \emph{redirection} is the fraction of GRASP timeouts followed by placing a \emph{different} orange. Both need an explicit target, so they are subtask-only --- the monotask cells are \emph{pending} a per-step proximity log (its reconstructed target flickers too much to count). \emph{Empty episodes} (0/3 placed) is the fair, geometric outcome for both formulations.}
    \label{tab:redirection}
    \begin{tabular}{llccc}
        \toprule
        Training set & Formulation & Oranges tried/ep & Redirection & Empty episodes \\
        \midrule
        Teleop      & Monotask & \emph{pending} & --- & 43\% \\
        Teleop      & Subtask  & 2.7 & 31\% & 3\% \\
        \midrule
        Teleop+Auto & Monotask & \emph{pending} & --- & 27\% \\
        Teleop+Auto & Subtask  & 2.8 & 20\% & 16\% \\
        \bottomrule
    \end{tabular}
\end{table}

\textbf{What we cannot yet attribute.} The empty-episode gap is consistent with redirection, but we cannot yet prove the end-to-end policy literally \emph{fixates} --- dwelling next to one orange without closing on it --- rather than just grasping more poorly overall, because its target is reconstructed heuristically (hence the \emph{pending} cells). Distinguishing the two needs per-step gripper--orange proximity, the first upgrade to the evaluation (\cref{sec:discussion}).

\subsection{Target Obedience in the Subtask Policies}
\label{sec:obedience}

Only the orchestrated runs name their target by spatial label (``Grasp \emph{<position>} orange''), making it possible to ask whether the policy grasps the orange it was \emph{told} to --- a question monotask runs cannot pose. A grasp is \textbf{obeyed} when the gripper closes on the requested orange (matched by identity, not position label), counted over grasps that engage an orange; any other engaged grasp is a \textbf{misgrab}. Obedience is reported by \textbf{scene state} (oranges already placed at grasp time), since resolving a referent depends on how many distractors remain.

\paragraph{The instructions are dominated by one referent.} \Cref{tab:label_prevalence} counts training GRASP episodes per label: \emph{right} alone is almost half, with \emph{middle} and \emph{left} next and diagonal labels rare; the Auto episodes only deepened this skew. This sets up a clean test: \textbf{if obedience were limited by exposure, the most common labels should be obeyed best.}

\begin{table}[H]
    \centering
    \small
    \caption{Training GRASP episodes per positional label, by source. ``Other'' aggregates the rare top-left, top-middle, and bottom-middle labels.}
    \label{tab:label_prevalence}
    \begin{tabular}{lccc}
        \toprule
        Position & Teleop & Auto & Combined \\
        \midrule
        top right    & 25  & 4   & 29  \\
        left         & 34  & 11  & 45  \\
        middle       & 74  & 48  & 122 \\
        right        & 135 & 74  & 209 \\
        bottom right & 15  & 1   & 16  \\
        other        & 7   & 5   & 12  \\
        \midrule
        Total        & 290 & 143 & 433 \\
        \bottomrule
    \end{tabular}
\end{table}

\paragraph{Obedience improves as the scene simplifies.} \Cref{fig:grasp_confusion} reports, per model and scene state, how often each requested position was obeyed (green diagonal) and where misses landed (red). Roughly three in four engaged grasps reach the requested orange, and the rate climbs as oranges clear: lowest with all three present, higher once one is placed, trivially perfect with two placed. \textbf{Fewer distractors make the referent easier to resolve.}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{grasp_obedience_confusion.pdf}
    \caption{Grasp-obedience confusion matrices for the two subtask models: three oranges present (top), one already placed (bottom). Rows are the requested position, columns the grasped one (by identity); \textbf{green} = obeyed, \textbf{red} = misgrab. The trivial two-placed state and single-sample requests are omitted.}
    \label{fig:grasp_confusion}
\end{figure}

\paragraph{Misgrabs follow a systematic inward bias.} The errors are far from random: in every panel of \Cref{fig:grasp_confusion} a missed request closes on the orange immediately \textbf{inward of} the intended one --- \emph{right} taken as \emph{middle}, \emph{middle} as \emph{left}, \emph{bottom right} as \emph{top right}. The extreme referents \emph{left} and \emph{top right} are thus obeyed most reliably, while \emph{right} is the worst-obeyed label in both models and scene states. This is real behaviour, not a logging artefact: the grasped orange is identified from contact, the requested one is verifiably the true rightmost, and the orange taken is a physically distinct one a few centimetres inward --- a genuine short reach in packed scenes.

\paragraph{A grounding gap, not a coverage gap.} This ordering runs \emph{opposite} to training prevalence: the most frequent label \emph{right} is the worst obeyed, the sparse \emph{top right} among the best. \textbf{More examples do not buy better obedience} --- exactly what the Auto data shows, adding exposure without lifting it. The bottleneck is \emph{grounding} the instruction to the scene: the VLA's positional conditioning is a spatially biased channel, not a clean target selector --- the context-dependence the scripted spatial reset works around.
