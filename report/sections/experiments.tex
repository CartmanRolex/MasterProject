\section{Results}
\label{sec:experiments}

This section reports the experimental evidence collected so far. The emphasis is on factual comparisons between task formulations and training sources. The dataset names used throughout are: \textbf{LightwheelAI Baseline} for the 60-episode \texttt{leisaac-pick-orange-mimic-v0} demonstrations published by LightwheelAI, \textbf{Teleop} for the manually collected subtask dataset, \textbf{Auto} for the balanced automatically generated successful subtasks, and \textbf{Teleop+Auto} for their merged training set.

The current evaluations use the same reference seed set, \texttt{pick\_orange\_reference\_100\_v1}, for every completed model run. Each run therefore contains the same 100 initial conditions in the same order; across seed indices, the randomized quantities are the top-camera pose and the orange positions. This makes differences between bars more directly attributable to the policy and task formulation rather than to easier or harder sampled scenes.

\subsection{Full-Task Outcomes Across Formulations}

\Cref{fig:orange_outcome_distribution} reports final plate occupancy for every evaluated model on the shared 100-seed reference set, under an identical 5000-step episode budget. Two complementary metrics are read from it. The \emph{full-task success rate} is the fraction of episodes ending with all three oranges in the plate; it measures whether the long-horizon task is actually solved. The \emph{mean final occupancy} is the average number of oranges placed out of three; it credits partial progress and so separates models that make headway from those that stall early. Bars are grouped by training source --- LightwheelAI Baseline, Teleop, and Teleop+Auto --- and, within the two custom sources, by task formulation: a \emph{monotask} policy run end-to-end versus an orchestrated \emph{subtask} policy.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{orange_outcome_distribution.pdf}
    \caption{Final plate occupancy across evaluated task formulations. Completed bars use the same 100 seeded initial conditions. Bar segments are percentages of evaluation episodes ending with 0, 1, 2, or 3 oranges in the plate; \textit{mean} is average final oranges in plate out of three. Bars are grouped by training dataset and labelled by task formulation.}
    \label{fig:orange_outcome_distribution}
\end{figure}

\paragraph{Policy backbone.} \textbf{[Placeholder: interpretation of the ACT vs.\ SmolVLA baseline comparison to be finalised.]}

\paragraph{Subtasking improves over monotasking.} On the same training trajectories, the orchestrated subtask formulation outperforms the monotask one on both metrics. The effect is clearest on Teleop, where decomposition roughly doubles full-task success (13\% to 29\%) and lifts mean occupancy from 0.92 to 1.80, indicating that explicit subtask checkpoints convert otherwise-stalled episodes into progress. On Teleop+Auto the advantage narrows and splits between the two metrics: the subtask model retains a slight edge in mean occupancy (1.47 vs.\ 1.42) but a lower full-task success rate (13\% vs.\ 26\%). Decomposition therefore reliably helps a policy make partial progress, but on the merged data it does not translate that progress into complete-task success --- the controller secures more oranges on average yet less often finishes all three.

\paragraph{Subtasking rarely fails completely.} The gap is most striking on the weakest outcome --- ending an episode with no orange placed at all. The orchestrated subtask models almost always place at least one orange (89\% of episodes on Teleop, 84\% on Teleop+Auto), whereas the monotask models leave a large fraction of episodes empty (only 50\% reach one orange on Teleop, 69\% on Teleop+Auto). A plausible explanation lies in the orchestrator's target redirection: a monotask policy that fixates on a single hard-to-grasp orange can burn the entire episode without ever placing one, while the controller detects a repeated GRASP timeout, abandons that orange, and redirects to another. The decomposed formulation thus rarely produces a total failure, even when it does not complete the full task.

\paragraph{Aggregating autonomous data did not help.} Adding the automatically generated episodes to Teleop did not strengthen the policy. Under the orchestrated subtask formulation it in fact regressed performance on both metrics relative to Teleop alone (full-task success 29\% to 13\%, mean occupancy 1.80 to 1.47). Augmenting the dataset with in-distribution successful subtasks was thus not sufficient to improve full-task behaviour; the subtask-level analysis below isolates which behaviours degraded.

\subsection{Lift Failures and Recovery}
\label{sec:lift_recovery}

Beyond the final outcome, the per-step traces show how each policy reacts when a grasped orange slips during the lift. A \emph{lift} begins once a held orange rises above its initial height and \emph{drops} if it leaves the gripper before reaching the plate. \Cref{tab:lift_recovery} reports the per-attempt lift success rate and what follows a drop, for all four models. (The monotask traces are observer-inferred from physics, the subtask traces are the orchestrator's own log; the comparison should be read with that asymmetry in mind.)

\begin{table}[H]
    \centering
    \small
    \caption{Lift outcomes and post-drop behaviour, by training set and task formulation. Lift success is per genuine attempt (successes / successes+drops, excluding episode-end truncations). \emph{Drop recovered}: fraction of oranges whose first lift dropped that were nonetheless lifted later in the episode (a placed orange counts as lifted). \emph{Re-engaged same orange}: fraction of drops whose next lift attempt targeted the same orange rather than a different one.}
    \label{tab:lift_recovery}
    \begin{tabular}{llccc}
        \toprule
        Training set & Formulation & Lift success & Drop recovered & Re-engaged same orange \\
        \midrule
        Teleop      & Monotask & 61.2\% (145/237) & 26.0\% (19/73) & 50.0\% (32/64) \\
        Teleop      & Subtask  & 84.4\% (216/256) & 72.7\% (24/33) & 83.9\% (26/31) \\
        \midrule
        Teleop+Auto & Monotask & 83.5\% (167/200) & 14.3\% (4/28)  & 25.0\% (4/16)  \\
        Teleop+Auto & Subtask  & 85.5\% (194/227) & 38.5\% (10/26) & 63.2\% (12/19) \\
        \bottomrule
    \end{tabular}
\end{table}

The decisive difference is not the raw lift rate, which is comparable except for the weak Teleop monotask model, but the response to a drop. The subtask policies re-engage the dropped orange far more often than the monotask ones (83.9\% vs.\ 50.0\% on Teleop; 63.2\% vs.\ 25.0\% on Teleop+Auto) and so recover a much larger share of dropped oranges (72.7\% vs.\ 26.0\%; 38.5\% vs.\ 14.3\%). This is the local-retry mechanism made visible: the orchestrator returns to GRASP for the same orange, whereas the monotask policy drifts to a different one and rarely reclaims the dropped orange --- abandoning three of four on Teleop+Auto. When recovery does happen it is near-immediate (a single retry on average in every model), so the gap is driven by \emph{whether} the same orange is re-attempted, not how persistently.

\subsection{Target Obedience in the Subtask Policies}
\label{sec:obedience}

Only the orchestrated runs request a specific orange by spatial label (``Grasp \emph{<position>} orange''), so obedience --- did the policy grasp the orange it was told to --- is a subtask-only question, which we relate to how often each referent appears in training.

\paragraph{Instruction prevalence.} \Cref{tab:label_prevalence} counts the GRASP episodes per positional label. The distribution is heavily skewed: \emph{right} alone is almost half of all grasp instructions (135/290 Teleop, 209/433 combined), then \emph{middle} and \emph{left}; the compound diagonal labels are rare. Adding Auto did not rebalance it --- it reinforced the same head, contributing mostly more \emph{right} (74) and \emph{middle} (48).

\begin{table}[H]
    \centering
    \small
    \caption{Prevalence of positional grasp instructions in the training data: number of GRASP episodes per label, by source. ``Other'' aggregates the rarely used top left, top middle, and bottom middle labels.}
    \label{tab:label_prevalence}
    \begin{tabular}{lccc}
        \toprule
        Position & Teleop & Auto & Combined \\
        \midrule
        top right    & 25  & 4   & 29  \\
        left         & 34  & 11  & 45  \\
        middle       & 74  & 48  & 122 \\
        right        & 135 & 74  & 209 \\
        bottom right & 15  & 1   & 16  \\
        other        & 7   & 5   & 12  \\
        \midrule
        Total        & 290 & 143 & 433 \\
        \bottomrule
    \end{tabular}
\end{table}

\paragraph{Obedience.} A grasp is obedient when it engages the requested orange by identity, counted only over grasps that actually engaged one (timeouts are failures to grasp, not wrong-orange grasps). Overall the policies obey 75.1\% (193/257) of engaged grasps on Teleop and 77.0\% (187/243) on Teleop+Auto, but this rises as the scene empties --- from 65--69\% with all three oranges present (overall 83/127 and 100/145), to 75--85\% with one already placed (61/81 and 64/75), to a trivial 100\% with two placed (one orange remains, always labelled ``right'') --- because fewer distractors make the referent easier to resolve. \Cref{fig:grasp_confusion} shows, for both states, not just the obeyed rate (the diagonal) but where each missed grasp actually landed.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{grasp_obedience_confusion.pdf}
    \caption{Grasp-obedience confusion matrices for the two subtask models, with no oranges placed (top, all three on the table) and one placed (bottom, two remaining). Rows are the requested position, columns the position actually grasped by orange identity; the green-boxed diagonal is obeyed grasps and each cell is shaded by its row fraction. The obeyed rate and row count are printed beside each row. Labels are taken from the scene as it stood at grasp time; the 2-placed state is omitted (one orange remains, trivially obeyed), as are two single-sample requests.}
    \label{fig:grasp_confusion}
\end{figure}

The errors expose a single directional bias, consistent across both training sets and both states. \emph{right} is the worst-obeyed label (24--32\% with three present); when it is missed the policy almost always takes the \emph{middle} orange (19/29 and 21/38 with three present, and still 16/17 and 8/9 of misses once one is placed), just as \emph{middle} is misgrabbed as \emph{left} and \emph{bottom right} as its close-pair \emph{top right} --- the arm consistently grasps an orange to the \emph{left of, or inward from}, the requested one, while the outermost \emph{left} and \emph{top right} are obeyed 88--97\%. This is not a logging artefact: the grasped orange is identified from contact forces, in every \emph{right}$\to$\emph{middle} case the requested orange is verifiably the true rightmost, and the substitute is a genuinely distinct orange (median 10\,cm away), so the confusion reflects a real $\sim$4\,cm under-reach in tightly packed scenes (median inter-orange gap 32\,mm), not mislabelling. Crucially the obedience ordering runs \emph{opposite} to training prevalence: the most represented label, \emph{right}, is the worst obeyed, the sparse \emph{top right} the best. The deficit is therefore not a coverage gap that more examples could close --- as the Auto data confirms --- but a grounding one: the VLA's language conditioning is a spatially biased channel rather than a clean selector, the context-dependence that motivates the scripted spatial reset.
