# Work log — active Claude sessions & long jobs

Lightweight coordination so parallel sessions (and you) can see who owns what.
Each session: add a row when you start, update status when you finish. Keep it short;
this is a scratch board, not history. See `WORKFLOW.md` for the full playbook.

| Branch / worktree | Scope (one task) | Mode | Status | Updated |
|-------------------|------------------|------|--------|---------|
| `main`            | —                | interactive | — | — |
| _e.g._ `cc/eval-rerun` | re-run seeded evals to populate v2 fields | unattended (tmux cc-eval-rerun) | running | 2026-06-24 |

## Long-running / GPU jobs

Only one GPU-heavy job at a time (single 5090). Note the holder here.

- GPU holder: _none_
